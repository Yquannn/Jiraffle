server:
  port: 8081

spring:
  application:
    name: ads-service
  datasource:
    url: jdbc:postgresql://postgres:5432/jiraffle_ads
    username: ${DB_USER:postgres}
    password: ${DB_PASSWORD:postgres}
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true